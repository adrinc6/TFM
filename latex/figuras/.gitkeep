# Carpeta para figuras y tablas exportadas por el pipeline (Fase 5, module/report.py).
# Se referencian desde los capítulos con \includegraphics; ruta configurada en main.tex
# mediante \graphicspath{{figuras/}}. Este .gitkeep solo mantiene la carpeta en git.
